{
  "name": "zou-demo-functions",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "install:app": "npm --prefix app ci --legacy-peer-deps && npm --prefix app/node_modules/ajv-keywords install ajv@^8 --no-save --no-package-lock",
    "serve:app": "node serve-app.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
