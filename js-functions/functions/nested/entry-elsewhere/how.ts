// Beside the entrypoint rather than beside a directory named after the
// function, which is the other half of the question: whatever a
// function like this imports has to come with it.
export const how = "an entrypoint somewhere else";
