{
  "name": "zou-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "stub": "node stub-github.mjs",
    "install:app": "npm --prefix app ci --legacy-peer-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
