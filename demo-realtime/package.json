{
  "name": "zou-demo-realtime",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "install:app": "npm --prefix app ci --legacy-peer-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "sass": "^1.80.0"
  }
}
